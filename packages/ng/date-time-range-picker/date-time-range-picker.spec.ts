import { MznDateTimeRangePicker } from './date-time-range-picker.component';

// Smoke spec — DateTimeRangePicker is a CVA wrapper.
describe('DateTimeRangePicker', () => {
  it('should export MznDateTimeRangePicker', () => {
    expect(MznDateTimeRangePicker).toBeDefined();
  });
});
