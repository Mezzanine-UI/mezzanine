import { MznTimeRangePicker } from './time-range-picker.component';

// Smoke spec — TimeRangePicker is a CVA wrapper.
describe('TimeRangePicker', () => {
  it('should export MznTimeRangePicker', () => {
    expect(MznTimeRangePicker).toBeDefined();
  });
});
