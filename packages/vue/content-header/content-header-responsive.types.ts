import type { ResponsiveBreakpoint } from '@mezzanine-ui/core/content-header';

export interface ContentHeaderResponsiveProps {
  /**
   * The breakpoint at which the wrapped children are shown.
   */
  breakpoint: ResponsiveBreakpoint;
}
