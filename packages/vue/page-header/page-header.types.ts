/**
 * PageHeader takes nothing but its content, which is a slot here rather than a
 * prop. React's own type is `children` plus the native header attributes.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface PageHeaderProps {}
