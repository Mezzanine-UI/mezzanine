/**
 * NavigationFooter takes nothing but its content, which is a slot here rather
 * than a prop. React's own interface is `children` plus the native footer
 * attributes.
 */
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface NavigationFooterProps {}
