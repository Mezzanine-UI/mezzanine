import type { TransitionImplementationProps } from './transition.types';

/**
 * Fade takes the shared transition props unchanged, exactly as React's
 * `FadeProps = TransitionImplementationProps` does.
 */
export type FadeProps = TransitionImplementationProps;
