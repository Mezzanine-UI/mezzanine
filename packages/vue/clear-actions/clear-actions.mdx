import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as ClearActionsStories from './clear-actions.stories';

<Meta of={ClearActionsStories} />

# ClearActions

清除／關閉動作按鈕。

`type` 決定使用情境：`standard` 為獨立按鈕、`embedded` 內嵌於其他控制項、`clearable` 用於清除輸入內容。

[Source Code](https://github.com/Mezzanine-UI/mezzanine/blob/main/packages/vue/clear-actions/clear-actions.vue)

<ArgTypes of={ClearActionsStories.Playground} />
