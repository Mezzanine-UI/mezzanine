import { Meta, ArgTypes, Description } from '@storybook/addon-docs/blocks';
import DateRangePicker from './DateRangePicker';
import DateRangePickerCalendar from './DateRangePickerCalendar';
import * as DateRangePickerStories from './DateRangePicker.stories';

<Meta of={DateRangePickerStories} />

# DateRangePicker

<Description of={DateRangePicker} />

### For typescript users

When using date-range-picker, you should declare an exported `DateType` with your desired date library.
For example, having below code in your `index.d.ts`:

```ts
import { Moment } from 'moment';

declare module '@mezzanine-ui/core/calendar' {
  export type DateType = Moment;
}
```

[Source Code](https://github.com/Mezzanine-UI/mezzanine/blob/main/packages/react/src/DateRangePicker/DateRangePicker.tsx)

<ArgTypes of={DateRangePicker} />

# DateRangePickerCalendar

<Description of={DateRangePickerCalendar} />

[Source Code](https://github.com/Mezzanine-UI/mezzanine/blob/main/packages/react/src/DateRangePicker/DateRangePickerCalendar.tsx)

<ArgTypes of={DateRangePickerCalendar} />
