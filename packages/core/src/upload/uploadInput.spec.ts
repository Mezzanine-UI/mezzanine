import { uploadInputClasses as classes } from '.';

describe('uploadInput', () => {
  describe('uploadInputClasses', () => {
    it('prop:host', () => {
      expect(classes.host).toBe('mzn-upload-input');
    });
  });
});
