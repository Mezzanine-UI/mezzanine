import { switchPrefix } from './constants';

export const switchClasses = {
  host: switchPrefix,
  checked: `${switchPrefix}--checked`,
  disabled: `${switchPrefix}--disabled`,
  large: `${switchPrefix}--large`,
  control: `${switchPrefix}__control`,
  input: `${switchPrefix}__input`,
};
