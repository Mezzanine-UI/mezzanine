# React → Angular Parity Deviations

當 React 與 Angular 元件因平台差異無法達成完整 parity 時，**先停下來**向使用者說明原因，取得核可後在此檔新增一列。`tools/parity/compare.ts` 會解析下表，**只**抑制明列的 `(Component, Story, Kind)` 三元組 diff，其他一律視為失敗。

- `Component`：kebab-case，需與 `tools/parity/.out/<component>/` 對應
- `Story`：story 的 `name`（即 export 名稱）；填 `*` 代表該元件所有 story
- `Kind`：`tag` | `attr` | `style` | `text` | `missing` | `extra` | `api`
- `Reason`：必須具體說明為何無法 parity
- `Approved`：使用者核可日期 `YYYY-MM-DD`

| Component             | Story                     | Kind    | React                                                | Angular                                    | Reason                                                                                                                                                                                                                                                                                                                                            | Approved   |
| --------------------- | ------------------------- | ------- | ---------------------------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- |
| date-time-picker      | **api**                   | input   | fadeProps                                            | —                                          | React 用 `react-transition-group` / 自訂 Transition lib 暴露一整包 props 控制彈出層的淡入淡出動畫。Angular 這邊採用純 CSS transition，沒有同等的 JS 可配置物件，因此無法做出 prop-level 對應。若未來 Angular 側新增類似的 transition 層，應回頭加上 `fadeProps` input 並移除此行。                                                                | 2026-04-11 |
| upload                | **api**                   | input   | errorIcon / uploaderIcon(各 ReactNode 欄位)          | IconDefinition + ng-content                | React 允許傳入任意 `ReactNode` 作為 icon。Angular 無對等任意模板傳遞機制，改以 `IconDefinition` Input 輔以 `<ng-content>` 投影提供客製化路徑。覆蓋率足以滿足目前 design system icon 使用場景。                                                                                                                                                    | 2026-04-14 |
| upload                | **api**                   | output  | onUpload                                             | uploadHandler + upload                     | React `onUpload` 同時具有 async return value（控制 files 狀態）與 `setProgress` callback。Angular EventEmitter 不接收 return value，因此拆成 `uploadHandler: UploadHandler` Input（主要控制路徑，含 setProgress）+ `upload: File[]` Output（副通知）。兩者並存時以 handler 為準。                                                                 | 2026-04-14 |
| upload                | **api**                   | input   | controllerRef                                        | exportAs='mznUploader' + public methods    | React 的 imperative controller ref 在 Angular 以 `exportAs` + `click()` / `reset()` 公開方法替代；功能等價。                                                                                                                                                                                                                                      | 2026-04-14 |
| upload                | **api**                   | input   | inputRef                                             | @ViewChild('inputRef')                     | Angular 以 `exportAs='mznUploader'` 暴露 `inputRef` getter 取得 native `<input>` 元素，消費端透過 template ref 取用。                                                                                                                                                                                                                             | 2026-04-14 |
| upload                | **missing**               | style   | —                                                    | —                                          | `.mzn-upload-item__error-message` 在 React 是 `.mzn-upload-item` host 的 **兄弟節點**（React Fragment），Angular 以屬性選擇器 `[mznUploadItem]` 僅能單一 host 容納所有子節點，因此 error-message 改置於 host 內部末端。視覺上一致，但 DOM 深度多一層。                                                                                            | 2026-04-14 |
| accordion             | **api**                   | input   | actions                                              | `<ng-content select="[actions]">`          | React `actions` prop 接收 `AccordionActionsProps`（含 ReactNode children），屬 React ReactNode → Angular content projection 的經典例外模式。Angular 以 `MznAccordionActions` directive 加 `actions` 標記屬性投影（`<div actions mznAccordionActions>`）達成等效，避免 TemplateRef input 與 content projection 兩套並存。                          | 2026-04-15 |
| accordion             | **api**                   | output  | onChange (→ `change`)                                | expandedChange                             | Angular 慣例以 `xxxChange` 後綴支援 `[(expanded)]` 兩向綁定，改名為 `change` 會讓使用者失去 banana-in-a-box 語法。EventEmitter 行為與 React `onChange(boolean)` 等價，僅命名不同，保留 Angular idiom 以兼顧 DX。                                                                                                                                  | 2026-04-15 |
| accordion             | With Actions              | attr    | aria-controls / aria-expanded / aria-haspopup        | —                                          | `<Dropdown>` 的觸發按鈕 a11y 連動屬性由 React Dropdown 元件自動注入到 trigger button。Angular `MznDropdown` 目前不提供此自動注入機制——屬 Dropdown 元件的差異，非 Accordion 本身。俟 Dropdown parity 完成後會自動消除此 diff。                                                                                                                     | 2026-04-15 |
| accordion             | With Actions              | extra   | —                                                    | `<div mznDropdown>` sibling                | React `<Dropdown>` 將 listbox 透過 Portal 渲染到 body；Angular `MznDropdown` 的 popper host div 留在 DOM 中（雖然內部 list 透過 portal 處理），故 accordion actions 內多一個 sibling div。屬 Dropdown 元件的差異，非 Accordion 本身。                                                                                                             | 2026-04-15 |
| card-group            | **api**                   | input   | loadingType                                          | cardType                                   | React `loadingType` 只在 `loading=true` 時指定，其餘情況由 CardGroup 從第一個 child 自動偵測群組類型（`firstCardType === FourThumbnailCard` 等）。Angular 無 runtime child-type 偵測機制，故以 `cardType` 明確表達「群組固定類型」的語意；功能等價但命名反映實作差異。                                                                            | 2026-04-15 |
| four-thumbnail-card   | **api**                   | input   | personalActionOnClick / active / event               | personalActionClick (output)               | React 使用非標準的 `personalActionOnClick` 命名（非 `onX`），parity extractor 將它視為 input 而非 output，同時把 callback 參數（`event`、`active`）當成獨立 input 欄位。Angular 依循慣例以 `personalActionClick: EventEmitter<{event, active}>` output 實作，語意等價、EventEmitter payload 包含兩個參數。                                        | 2026-04-15 |
| four-thumbnail-card   | **api**                   | output  | —                                                    | personalActionClick                        | 同上：React 是 `personalActionOnClick` callback input；Angular 等價以 output 實作。                                                                                                                                                                                                                                                               | 2026-04-15 |
| four-thumbnail-card   | Type: Overflow            | attr    | aria-controls / aria-expanded / aria-haspopup        | —                                          | MznDropdown trigger 的 a11y 連動屬性 React 自動注入到 trigger button，Angular 目前不會。屬 Dropdown 元件層級差異，與 Accordion WithActions 同源。                                                                                                                                                                                                 | 2026-04-16 |
| four-thumbnail-card   | Type: Overflow            | extra   | —                                                    | `<div mznDropdown>` sibling                | 同 Accordion WithActions 的 MznDropdown portal vs inline sibling 差異——React Dropdown 將 listbox 透過 Portal 渲染到 body，Angular MznDropdown 的 popper host div 留在 trigger 旁邊。                                                                                                                                                              | 2026-04-16 |
| single-thumbnail-card | **api**                   | input   | personalActionOnClick / active / event               | personalActionClick (output)               | 與 four-thumbnail-card 同源：React 的 `personalActionOnClick` 不是 `onX` 命名，parity extractor 將它視為 input + 把 callback 參數當成獨立 input 欄位。Angular 以 output `personalActionClick: EventEmitter<{event, active}>` 實作。                                                                                                               | 2026-04-16 |
| single-thumbnail-card | **api**                   | output  | —                                                    | personalActionClick                        | 同上。                                                                                                                                                                                                                                                                                                                                            | 2026-04-16 |
| single-thumbnail-card | Type: Overflow            | attr    | aria-controls / aria-expanded / aria-haspopup        | —                                          | 與 four-thumbnail-card / accordion 同源的 MznDropdown a11y 自動注入差異。                                                                                                                                                                                                                                                                         | 2026-04-16 |
| single-thumbnail-card | Type: Overflow            | extra   | —                                                    | `<div mznDropdown>` sibling                | 與 four-thumbnail-card / accordion 同源的 Dropdown portal vs inline sibling 差異。                                                                                                                                                                                                                                                                | 2026-04-16 |
| description-title     | **api**                   | input   | —                                                    | text                                       | Angular 額外提供 `text` input，讓消費端可直接以 prop 傳入標題字串（方便程式化使用與 badge 模式整合）；React 只接受 `children`。兩者語意等價，Angular 的 `text` 是 content-projection 以外的便利 API。                                                                                                                                             | 2026-04-16 |
| media-preview-modal   | **api**                   | input   | disableCloseOnBackdropClick default=false            | disableCloseOnBackdropClick default=true   | Angular 專案要求媒體預覽浮層只能由 X 按鈕關閉，避免使用者檢視放大圖片時不小心點到遮罩就把 modal 關掉。React 的 `false` 保留標準 modal 體驗，Angular 改以 `true` 作為 component-level 預設；API 形狀相同，`input()` 可被呼叫端覆寫。                                                                                                               | 2026-04-18 |
| media-preview-modal   | With Next Image Component | missing | button                                               | —                                          | React 提供此 story 示範與 `next/image` 互通，屬 React 生態系（Next.js）特有。Angular 沒有對應的官方 Image 元件，story 刻意不移植；若未來 Angular 有等價方案再補。                                                                                                                                                                                 | 2026-04-18 |
| checkbox              | **api**                   | input   | editableInput.onChange                               | editableInputChange (output)               | React bundle 內的 `onChange: ChangeEventHandler<HTMLInputElement>` 是 function prop；Angular 依慣例拆為 `editableInputChange: EventEmitter<string>` output 支援 `(editableInputChange)` 綁定（與 accordion `expandedChange` 同源的 xxxChange 慣例）。payload 由 Event 改為 value string，因為 Angular 不需要直接暴露原生 Event。                  | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.inputRef                               | —                                          | React 允許透過 `editableInput.inputRef` 取得 editable input 的原生 `<input>` ref（常與 react-hook-form `register` 搭配）。Angular 等價做法應是 `exportAs='mznCheckbox'` + public getter，目前尚未實作；template ref `#editableInputEl` 指向 `<div mznTextField>` host，消費端需自行 `querySelector('input')` 取到原生 input 元素。                | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.inputProps                             | —                                          | React 的 `inputProps` 接受任意 HTMLInputElement attributes（data-\*、aria-\*、onFocus 等）轉發至原生 `<input>`。Angular 的 `CheckboxEditableInput` bundle type 未定義此欄位，因此無對應傳遞路徑；需要時應擴充 bundle 型別並於 template 把欄位展開至 `<input>`。                                                                                   | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.onClear                                | editableInputChange emit('')               | React `editableInput.onClear` 是獨立 callback，與 `onChange` 分開觸發。Angular bundle 沒 `onClear` 對應欄位；`<div mznTextField>` 的 `(cleared)` 事件在元件內統一映射為 `editableInputChange` 發送空字串，行為等價但 API 形狀不同。                                                                                                               | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.warning                                | —                                          | React bundle 有 `warning: boolean` 控制橙色警示邊框。Angular 的 `MznTextField` 原生支援 `warning` input，但目前 `CheckboxEditableInput` bundle 未把此欄位納入；需要時擴充 bundle type 並於 template 把 `cfg.warning` 綁到 `<div mznTextField [warning]="...">`。                                                                                  | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.className                              | —                                          | React bundle 允許透過 `className` 加 class。Angular 的慣例是以 host class composition 表達樣式覆寫（或 `[ngClass]` 於消費端包裝層），不把 className 納入 bundle input — 與 CLAUDE.md 排除的一般性 `className` / `classes` API 同類。                                                                                                              | 2026-04-20 |
| checkbox              | **api**                   | input   | editableInput.prefix / suffix (ReactNode)            | prefixText + prefixIcon / suffixText       | React 的 `prefix`/`suffix` 接受任意 ReactNode（文字、icon、element）。Angular 無法在 bundle 物件內部再做 content projection，因此以 `prefixText`、`prefixIcon`（IconDefinition）、`suffixText` 作為 bridge。覆蓋 design system 目前所有使用情境。                                                                                                 | 2026-04-20 |
| checkbox-group        | **api**                   | input   | level.onChange: ChangeEventHandler<HTMLInputElement> | level.onChange: (checked: boolean) => void | React bundle 的 `onChange` 是原生 event handler，Angular 改傳 `boolean`（新的 checked 狀態）。Angular 慣例不把原生 `Event` 放進 bundle callback（MznCheckbox 本身也不 output native Event，仰賴 CVA + [checked] signal）。React 原生使用也只讀 `event.target.checked`，語意等價。與 `accordion.onChange → expandedChange` 同源的 xxxChange 慣例。 | 2026-04-21 |
